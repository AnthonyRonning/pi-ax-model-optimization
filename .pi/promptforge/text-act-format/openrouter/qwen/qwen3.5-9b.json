{
  "schemaVersion": 1,
  "description": "PromptForge Text Act Format adapter for coding-agent turns.",
  "instructions": [
    "Always emit tool calls using the exact syntax `<tool_call name=\"TOOL_NAME\">{...}</tool_call>` where the tool name is in the `name` attribute and the body is a valid JSON object—never use key=value style, never embed the tool name inside the tag body, and never leave the tag unclosed or malformed (e.g., `<tool_call>\\nread path=\"...\"` is always wrong).",
    "Before emitting any tool call, check the provided `availableTools` list; if the required tool is not present, emit `<blocked>` with a brief explanation instead of attempting the call.",
    "When the task is to inspect or read a known file (including project-context files like README.md or AGENTS.md), prefer `<tool_call name=\"read\">{\"path\":\"...\"}` over a `bash` command.",
    "For multiple tool calls, emit each as a fully closed, separate block before starting the next one.",
    "Use `<ask_user>` only when information needed to form the call is genuinely missing from the task description."
  ],
  "optimizer": {
    "kind": "ax-gepa",
    "createdAt": "2026-04-25T18:24:28.604Z",
    "targetId": "text-act-format.instructions",
    "bestScore": 0.8080277777777777,
    "optimizerType": "GEPA",
    "optimizationTime": 145630,
    "totalRounds": 4,
    "converged": true
  },
  "paretoFront": [
    {
      "instruction": "Emit explicit Text Act Format blocks instead of describing intended actions in prose.\nUse <tool_call name=\"tool_name\">{...}</tool_call> with a valid JSON object whenever the task requires inspecting, searching, executing, or mutating repo state.\nEvery tool call must include the name attribute on the opening tag, a JSON object body, and a closing </tool_call>; never write `<tool_call> read path=\"README.md\"`.\nFor multiple tool calls, emit multiple complete closed blocks; never start another <tool_call> before closing the previous one.\nSelect tools by intent: read/open/inspect a file => read; search/list/run/git/test/shell commands => bash; exact replacement in an existing file => edit; create or overwrite a file => write.\nNever use bare tool tags such as <read>, <bash>, <edit>, or <write>, and never put the tool name only inside the JSON body.\nUse <ask_user> when required information is missing and <blocked> when the tool surface cannot satisfy the task.\nAfter a tool_result, ground the next response in that result and avoid repeating the same tool call unless new information is required.",
      "scores": {
        "actParsing": 0.7776388888888888,
        "toolSelection": 0.7541666666666667,
        "completion": 0.7633333333333333,
        "toolGrounding": 0.8888888888888888,
        "failureAvoidance": 0.8122222222222221
      },
      "dominatedSolutions": 0
    },
    {
      "instruction": "Always emit tool calls using the exact syntax `<tool_call name=\"TOOL_NAME\">{...}</tool_call>` where the tool name is in the `name` attribute and the body is a valid JSON object—never use key=value style, never embed the tool name inside the tag body, and never leave the tag unclosed or malformed (e.g., `<tool_call>\\nread path=\"...\"` is always wrong).\nBefore emitting any tool call, check the provided `availableTools` list; if the required tool is not present, emit `<blocked>` with a brief explanation instead of attempting the call.\nWhen the task is to inspect or read a known file (including project-context files like README.md or AGENTS.md), prefer `<tool_call name=\"read\">{\"path\":\"...\"}` over a `bash` command.\nFor multiple tool calls, emit each as a fully closed, separate block before starting the next one.\nUse `<ask_user>` only when information needed to form the call is genuinely missing from the task description.",
      "scores": {
        "actParsing": 0.8431944444444444,
        "toolSelection": 0.7397222222222222,
        "completion": 0.7633333333333333,
        "toolGrounding": 0.8888888888888888,
        "failureAvoidance": 0.8049999999999999
      },
      "dominatedSolutions": 0
    }
  ]
}
